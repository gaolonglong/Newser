<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="textColor">@color/textColor_day</item>
        <item name="descriptionTextColor">@color/desTextColor_day</item>
        <item name="cardColor">@color/cardColor_day</item>
        <item name="footerColor">@color/footerColor_day</item>
        <item name="recyclerColor">@color/recyclerColor_day</item>
    </style>

    <style name="AppThemeNight" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary_night</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_night</item>
        <item name="colorAccent">@color/colorAccent_night</item>

        <item name="textColor">@color/textColor_night</item>
        <item name="descriptionTextColor">@color/desTextColor_night</item>
        <item name="cardColor">@color/cardColor_night</item>
        <item name="footerColor">@color/footerColor_night</item>
        <item name="recyclerColor">@color/recyclerColor_night</item>
    </style>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <!--提示框是否有边框-->
        <item name="android:windowFrame">@null</item>
        <!--提示框是否是浮动的-->
        <item name="android:windowIsFloating">true</item>
        <!--提示框是滞是透明的-->
        <item name="android:windowIsTranslucent">false</item>
        <!--提示框是否有标题-->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">#7f000000</item>
        <!--提示框的背景颜色是什么-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否充许对话框的背景变暗。为true则充许变暗-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!--底部对话框弹出和退出动画-->
    <style name="style_bottom_dlg_anim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/push_bottom_out</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
